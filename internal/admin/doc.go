// Package admin defines protected operator endpoint contracts.
//
// It does not bind listeners. The composition root decides how to expose these
// handlers, and the default production boundary must not be the public client
// listener.
package admin
